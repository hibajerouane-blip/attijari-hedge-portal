# Secret de signature du cookie de session (min. 32 caractères recommandé)
SESSION_SECRET=hedgedesk-demo-secret-change-me-in-prod-32b

# Optionnel — stub pour une future API BAM (non branchée)
# BAM_API_KEY=
